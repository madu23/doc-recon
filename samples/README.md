# Synthetic samples

Run `python scripts/generate_synthetic_samples.py` to regenerate the workbook, labels, and POD-like PNGs. Every identity and reference is deliberately synthetic. Cases cover exact and reordered names, fallback behavior, conflicting identity/reference evidence, missing completion fields, unreadable input, and a missing file.
